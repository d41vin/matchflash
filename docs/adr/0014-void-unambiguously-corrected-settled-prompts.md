# Void unambiguously corrected settled prompts

If a source correction unambiguously affects a settled prompt, MatchFlash voids every prediction for that prompt, removes the awarded points from all affected standings, and adds a recap data-quality note. Ambiguous late corrections are held for manual review in Convex rather than silently changing a fan result.
