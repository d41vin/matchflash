# Gate formation visualization on `unitId` verification

The Phase 2 schematic formation layer may ship only after live or devnet observations establish what TxLINE `unitId` values mean. If that contract is not verified, MatchFlash ships the complete Phase 1 field visualization without inferred formations or tactical labels.
