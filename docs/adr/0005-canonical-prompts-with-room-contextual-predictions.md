# Canonical prompts with room-contextual predictions

A prediction prompt is created once for its fixture-level Flash Card and appears in every unfrozen room. A user may answer it once; the recorded room is the social context used for room standings, not a distinct prompt or settlement scope. This keeps the timed moment and outcome consistent across MatchFlash while preventing conflicting cross-room answers.
