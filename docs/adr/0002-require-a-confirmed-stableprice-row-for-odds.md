# Require a confirmed StablePrice row for odds

Win probabilities and odds-swing Flash Cards must use the empirically confirmed StablePrice consensus market row. Until discovery establishes its exact market and bookmaker values, MatchFlash omits those odds-derived features rather than silently substituting a bookmaker-specific line, preserving honest provenance.
