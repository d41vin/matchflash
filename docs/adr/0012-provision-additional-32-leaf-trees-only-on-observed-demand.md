# Provision additional 32-leaf trees only on observed demand

The initial active Bubblegum V2 tree has 32 leaves. If it is exhausted, MatchFlash may provision another 32-leaf tree after an explicit operational decision based on actual demand; the capacity message must state that the current run is fully claimed and must not promise replenishment before a new tree exists.
