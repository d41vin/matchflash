# Use typed prompt templates for predictions

Phase F predictions are created only from a small typed template catalog. Every template has a machine-readable rule key, fixed options, a closing condition, a settlement event or window, and a void rule; free-text `settlementRule` copy describes the rule to fans but is never the mechanism that determines outcomes.
